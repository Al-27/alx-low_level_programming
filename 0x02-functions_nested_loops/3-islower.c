#include "main.h"
/**
 * _islower - Makes the sum of two numbers
 * @c: First operand
 * @arg2: Second operand
 *
 * Return: The sum of the two parameters
 */
int _islower(int c){
	return c <= 'z' && c >= 'a' ? 1 : 0;
}
